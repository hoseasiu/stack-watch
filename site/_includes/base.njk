<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>{{ title or "StackWatch" }} — AI Infrastructure Intelligence</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600&family=IBM+Plex+Mono:wght@400;500&family=Instrument+Serif:ital@0;1&display=swap" rel="stylesheet">
<link rel="stylesheet" href="{{ '/static/style.css' | url }}">
</head>
<body>

<a class="skip-link" href="#main">Skip to content</a>

<header>
  <a class="logo" href="{{ '/' | url }}">
    <div class="logo-mark">
      <svg viewBox="0 0 12 12" aria-hidden="true"><circle cx="3" cy="3" r="2"/><circle cx="9" cy="3" r="2"/><circle cx="3" cy="9" r="2"/><circle cx="9" cy="9" r="2"/></svg>
    </div>
    <span class="logo-name">StackWatch</span>
  </a>
  <nav>
    <a href="{{ '/' | url }}" class="{{ 'active' if nav == 'dashboard' }}"{% if nav == 'dashboard' %} aria-current="page"{% endif %}>Dashboard</a>
    <a href="{{ '/org/' | url }}" class="{{ 'active' if nav == 'orgs' }}"{% if nav == 'orgs' %} aria-current="page"{% endif %}>Organizations</a>
    <a href="{{ '/tech/' | url }}" class="{{ 'active' if nav == 'tech' }}"{% if nav == 'tech' %} aria-current="page"{% endif %}>Frameworks</a>
    <a href="{{ '/sectors/' | url }}" class="{{ 'active' if nav == 'sectors' }}"{% if nav == 'sectors' %} aria-current="page"{% endif %}>Sectors</a>
    <a href="{{ '/signals/' | url }}" class="{{ 'active' if nav == 'signals' }}"{% if nav == 'signals' %} aria-current="page"{% endif %}>Signals</a>
    <a href="{{ '/methodology/' | url }}" class="{{ 'active' if nav == 'methodology' }}"{% if nav == 'methodology' %} aria-current="page"{% endif %}>Methodology</a>
  </nav>
  <div class="header-right">
    <div class="pulse-indicator">
      <div class="pulse-dot"></div>
      {{ orgs.length }} orgs tracked
    </div>
  </div>
</header>

<main class="page" id="main">
{{ content | safe }}
</main>

<footer>
  <span>StackWatch · open-source · <a href="https://github.com/">github.com/stackwatch</a></span>
  <span>{{ orgs.length }} orgs · {{ signals.length }} signals (last 7d)</span>
  <span><a href="{{ '/methodology/' | url }}">Methodology</a> · signals inferred from public sources only</span>
</footer>

</body>
</html>
